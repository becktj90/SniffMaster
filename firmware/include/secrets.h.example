#pragma once
/*  secrets.h.example — copy to secrets.h and fill in your private credentials. */

// ── WiFi (multi-network — tries each in order) ──────────────────────────────
#define WIFI_NUM_NETWORKS 1
static const char* WIFI_CREDS[][2] = {
  { "your_ssid", "your_password" },
};

// ── Adafruit IO ───────────────────────────────────────────────────────────
#define AIO_USERNAME  "your_aio_username"
#define AIO_KEY       "your_aio_key"

// ── OpenWeatherMap ─────────────────────────────────────────────────────────
#define OWM_API_KEY   "your_openweathermap_key"

// ── OpenAI ──────────────────────────────────────────────────────────────────
#define OPENAI_API_KEY "your_openai_api_key"
#define OPENAI_MODEL   "gpt-4o-mini"

// ── Blynk IoT ──────────────────────────────────────────────────────────────
#define BLYNK_TEMPLATE_ID   "your_blynk_template_id"
#define BLYNK_TEMPLATE_NAME "your_blynk_template_name"
#define BLYNK_AUTH_TOKEN    "your_blynk_auth_token"
